# Changelog

## 1.0.0 - 2026-07-31

- Added the versioned `course-gradebook` template package and SHA-256 fingerprint.
- Added normalized input schema, template/output validators, and QA report generation.
- Kept the old `.xls` asset path, Windows Excel COM generator, and original CLI parameters.
- No visual or core template structure changes were introduced; the old asset is byte-identical to the canonical template.
