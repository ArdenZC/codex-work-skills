# 整门课程编排器 1.0

整门课程编排器把一整套教材、PPT、讲义和课程要求先编排成可审计的课程级中间产物：教学素材清单、累计知识图谱、内容原生课次计划、语义视觉计划、理论—实践边界、批量 QA 和干净课程包。

它是 `HTML课件生成器/courseware-html-generator` 与 `实践课HTML生成器/practice-class-html-generator` 的上层编排器，不复制下游 renderer。页面数、布局、视觉和实践任务由教学问题、知识状态、素材价值与学生可观察成果决定，不能由固定页数或课程名分支决定。

## 当前阶段

- Skill：`whole-course-orchestrator` 1.0.0-alpha
- 阶段：`architecture review`
- 真实 UML failure benchmark：只冻结外部证据 manifest；本阶段不重跑 9 份 PPT × 16 theory + 16 practice

## 使用顺序

先阅读本文件、`AGENTS.md`、`通用提示词.md` 与 `SKILL.md`，再由 Agent 提供课程蓝图和 teaching questions，调用 `scripts/` 中的确定性工具进行结构化、校验、QA 和打包。

核心入口：

```powershell
python scripts/mine_teaching_assets.py --source-root <ppt-dir> --output-dir <asset-dir> --json
python scripts/build_knowledge_graph.py --course-json <course-map.json> --output-json <knowledge-graph.json> --json
python scripts/plan_sessions.py --course-json <course-map.json> --knowledge-graph <knowledge-graph.json> --assets <source-assets.json> --output-json <session-plans.json> --json
python scripts/review_whole_course.py --session-plans <session-plans.json> --practice-plans <practice-plans.json> --knowledge-graph <knowledge-graph.json> --assets <source-assets.json> --output-json <whole-course-qa.json> --json
```

自动化 QA 通过不等于教师人工教学验收通过；真实整课 retry 只能在架构评审后另行授权。
