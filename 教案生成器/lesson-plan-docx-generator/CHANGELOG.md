# Changelog

## 1.0.0 - 2026-07-31

- Added the versioned `lesson-plan` template package and SHA-256 fingerprint.
- Added input schema, template/output validators, and `qa-report.json` generation.
- Kept `assets/lesson-plan-template.docx` and the original CLI parameters as compatibility entries.
