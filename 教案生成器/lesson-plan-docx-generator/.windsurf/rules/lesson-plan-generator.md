---
trigger: always_on
---

先读取 `SKILL.md` 和 `通用提示词.md`，只按其中的 Lesson Skill 2.2.1 / Content Contract V2 和 QA 流程工作。先形成课程级项目/任务规划；逐课正文、9 阶段、评价 remarks、反思直接来自 JSON。明确需要实践工单时，每个 Practice Task 一一对应一个 2 学时 WorkOrder；明确不需要时只核算实践学时，不生成 handoff。使用受保护模板和真实校验，禁止 sparse input、固定旧套话、IT 默认内容、静默截断或手工改 DOCX。
